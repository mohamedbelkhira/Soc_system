import { PlusCircle } from "lucide-react";

import { Button } from "../ui/button";

export default function AddIcon() {
  return (
    <Button type="button" className="p-0 w-10 h-10">
      <PlusCircle />
    </Button>
  );
}
